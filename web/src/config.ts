export const API_BASE_URL = "localhost:5056";
const WS = "ws"; // Use "wss" when deploying the app
const HTTP = "http"; // Use "https" when deploying the app

export const API_URL = `${HTTP}://${API_BASE_URL}/api`;
export const WS_URL = `${WS}://${API_BASE_URL}/api/ws`;

